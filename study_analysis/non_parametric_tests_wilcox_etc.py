from numpy.random import seedfrom numpy.random import randnseed(1)data1 = 5 * randn(100) + 50data2 = 5 * randn(100) + 51data3 = 5 * randn(100) + 51## Mann-Whitney-U Test## Assumptions#### - Samples are i.i.d (especially no unique sample is in BOTH groups. Values may be the same, but they may not refer to the IDENTICAL sample)#    - Therefore k-folds may not be validated with Wilcox as one sample appears in each of the k-1 folds## - Data is unpaired## - At least 20 samples per groupfrom scipy.stats import mannwhitneyustat, p = mannwhitneyu(data1, data2)if p > (alpha := 0.05):    print('Same distribution (fail to reject H0) Statistics=%.3f, p=%.3f' % (stat, p))else:    print('Different distribution (reject H0) Statistics=%.3f, p=%.3f' % (stat, p))## Wilcoxon signed-rank test## Assumptions#### - Samples are i.i.d (especially no unique sample is in BOTH groups. Values may be the same, but they may not refer to the IDENTICAL sample)#    - Therefore k-folds may not be validated with Wilcox as one sample appears in each of the k-1 folds## - Data is paired## - At least 20 samples per groupfrom scipy.stats import wilcoxonstat, p = wilcoxon(data1, data2)if p > (alpha := 0.05):    print('Same distribution (fail to reject H0) Statistics=%.3f, p=%.3f' % (stat, p))else:    print('Different distribution (reject H0) Statistics=%.3f, p=%.3f' % (stat, p))## Kruskal-Wallis H Test## Assumptions#### - Samples are i.i.d (especially no unique sample is in BOTH groups. Values may be the same, but they may not refer to the IDENTICAL sample)#    - Therefore k-folds may not be validated with Wilcox as one sample appears in each of the k-1 folds## - Data is unpaired## - At least 20 samples per group## - More than 2 groups present (essentially a generalization of Mann-Whitney-U test. But cannot tell WHICH group differs)from scipy.stats import kruskalstat, p = kruskal(data1, data2, data3)## Friedmann Test## Assumptions#### - Samples are i.i.d (especially no unique sample is in BOTH groups. Values may be the same, but they may not refer to the IDENTICAL sample)#    - Therefore k-folds may not be validated with Wilcox as one sample appears in each of the k-1 folds## - Data is paired## - At least 20 samples per group## - More than 2 groups present (essentially a generalization of Wilcoxon Signed Rank test. But cannot tell WHICH group differs)from scipy.stats import friedmanchisquarestat, p = friedmanchisquare(data1, data2, data3)